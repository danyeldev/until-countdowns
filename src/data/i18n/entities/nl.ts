/**
 * PLACEHOLDER — no curated names yet, so every entity keeps its English title in this locale.
 * Fill from `keys.ts`: the key is `slugify(englishTitle)`, the value is the name people in this
 * language actually search for. Omit anything that has no established local name.
 */
import type { EntityNames } from "./index";

export const nl: EntityNames = {};
