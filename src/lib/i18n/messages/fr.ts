/**
 * PLACEHOLDER — this locale still serves the English catalogue.
 * Replace the whole file with a translated `Messages` literal; see `en/` for the reference.
 */
import { EN, type Messages } from "./en";

export const fr: Messages = EN;
